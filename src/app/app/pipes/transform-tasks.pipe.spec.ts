import { TransformTasksPipe } from './transform-tasks.pipe';

describe('TransformTasksPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformTasksPipe();
    expect(pipe).toBeTruthy();
  });
});
